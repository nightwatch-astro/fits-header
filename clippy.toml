# No project-specific lint thresholds are needed yet; clippy defaults apply. Add
# entries here if the parser grows functions that trip clippy's argument/complexity
# thresholds. Dependencies must stay pure-Rust and MSVC-safe (no C/system libs).
