# Keep this crate dependency-free and std-only. No project-specific lint
# thresholds are needed yet; defaults apply. Add entries here if the parser
# grows functions that trip clippy's argument/complexity thresholds.
